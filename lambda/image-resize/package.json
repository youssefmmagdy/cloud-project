{
  "name": "image-resize-lambda",
  "version": "1.0.0",
  "type": "module",
  "description": "Resize images on S3 upload (300x300) via sharp",
  "main": "index.js",
  "scripts": {
    "zip": "powershell -Command \"Compress-Archive -Path index.js,node_modules,package.json -DestinationPath function.zip -Force\""
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1048.0",
    "sharp": "^0.34.5"
  }
}
